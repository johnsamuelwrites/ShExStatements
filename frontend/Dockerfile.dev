# ShExStatements Frontend Dockerfile - Development
# Used for hot-reloading during development

FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy source code
COPY . .

# Expose Vite dev server port
EXPOSE 3000

# Run development server
CMD ["npm", "run", "dev", "--", "--host"]
